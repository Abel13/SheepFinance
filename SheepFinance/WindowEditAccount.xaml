<Window x:Class="SheepFinance.WindowEditAccount"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SheepFinance"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d" WindowStyle="None" WindowStartupLocation="CenterScreen" ResizeMode="NoResize" Background="LightGray" Width="300" Height="100" FontFamily="EngraversGothic BT">
    <StackPanel>
        <Grid Margin="1" Background="White" VerticalAlignment="Top" Width="300">
            <TextBlock Text="Editar Conta" Foreground="#FF073E6A" FontSize="22" Margin="10" VerticalAlignment="Center"/>
            <Button x:Name="ButtonClose" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="10">
                <materialDesign:PackIcon Kind="Close"/>
            </Button>
        </Grid>
        <StackPanel Orientation="Horizontal">
            <TextBox x:Name="TextBoxName" Width="200" Margin="15" materialDesign:HintAssist.Hint="Nome" VerticalAlignment="Bottom"/>
            <Button x:Name="ButtonAdd" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="10">
                <materialDesign:PackIcon Kind="Check"/>
            </Button>
        </StackPanel>
    </StackPanel>
</Window>
